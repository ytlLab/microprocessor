[課程範例連結](https://drive.google.com/drive/folders/19-dYrPnunDlhtFThW60KZjctXpvXV488?usp=sharing)

# 4×4薄膜鍵盤
## 原理說明
薄膜按鍵模組（hex keypad），有4×4或3×4（少了最右邊一行的A, B, C, D鍵），</br>
由16個按鍵（開關）交織而成。有些按鍵模組直接使用按鍵（微觸）開關組裝</br>
<img width="600" height="300" alt="image" src="https://github.com/user-attachments/assets/fcc1cd15-4ac4-42d6-83d6-414e3418f49c" /></br>
4×4按鍵模組有8個接腳，分成列、行兩組，可以接在微控器的任意8個接腳</br>

### 按鍵偵測與掃描原理
把4×4按鍵簡化成3×1，像下圖這樣串連三個開關，連接到同一個微控制器的輸入腳。</br>
<img width="600" height="200" alt="image" src="https://github.com/user-attachments/assets/d232603a-f0b6-4600-98a9-ceec758083a2" /></br>

假設開關的「行1」到「行3」輸入端全都輸入高電位，無論開關是否被按下，Arduino將接收到高電位（1）。</br>
為了檢測到其中按鍵被按下，程式必須依序將「行1」~「行3」腳位設定成低電位。</br>
<img width="600" height="200" alt="image" src="https://github.com/user-attachments/assets/d100958e-8100-460a-b3cb-06aaf8b0b6d5" /></br>

輪到「行2」腳輸入低電位，此時，微控器的輸入腳也將接收到低電位（0），</br>
由此可知連接「行2」的「開關B」被按下了。</br>
<img width="600" height="200" alt="image" src="https://github.com/user-attachments/assets/de47be06-2fca-4402-946e-ab23c5ec229f" /></br>

輪到「行3」腳輸入低電位，由於「開關C」未被按下，</br>
因此微控器的輸入腳接收到高電位（1）。</br>
<img width="600" height="200" alt="image" src="https://github.com/user-attachments/assets/3ee01df2-8791-4cd5-8a8c-8367dcd8d020" /></br>

到此，偵測按鍵的程式必須再次回到「行1」，輸入低電位…</br>
如此反覆循環掃描，才能持續偵測到某個按鍵是否被按下。</br>
實際的程式需要運用雙重迴圈，才能分批掃描每一列：</br>
<img width="664" height="359" alt="image" src="https://github.com/user-attachments/assets/e0de6714-4397-43af-b263-97e896decdf5" /></br>




以上出處：[超圖解物聯網IoT實作入門](https://swf.com.tw/?p=917)
## 接線方式
<img width="600" height="400" alt="image" src="https://github.com/user-attachments/assets/9f53e519-5b90-4413-8a03-8c5cb5ed2a9c" /></br>

## 安裝程式庫
工具 >> 管理程式庫... >> 搜尋 "keypad"  >> 安裝</br>
<img width="600" height="300" alt="image" src="https://github.com/user-attachments/assets/a13941a5-a265-46da-9771-cca3378d1767" /></br>

## 範例
```
//ch05_01
#include <Keypad.h>         // 鍵盤函式庫

const byte ROWS = 4;        // 宣告列為4，const為不能被更改，byte數值範圍0~255，占用1 byte記憶體
const byte COLS = 4;        // 宣告行為4，const為不能被更改，byte數值範圍0~255，占用1 byte記憶體
char keys[ROWS][COLS] = {   // 鍵盤對應字元
  {'1','2','3','A'},
  {'4','5','6','B'},
  {'7','8','9','C'},
  {'*','0','#','D'}
};
byte rowPins[ROWS] = {0,4,16,17};   // row接腳
byte colPins[COLS] = {5,18,19,21};  // col接腳
Keypad keynum = Keypad(makeKeymap(keys), rowPins, colPins, ROWS, COLS );  // 創建Keypad物件叫做keynum，並放入上述的資料

void setup(){
  Serial.begin(115200);
}

void loop(){
  char key = keynum.getKey();        // 讀取按鍵值並存入key
  if (key != NO_KEY){                // 判斷是否有按鍵值
    Serial.println(key);             // 在序列埠監控視窗顯示按鍵值
  }
}
```

# 程式整合
## 4×4薄膜鍵盤 + TrafficLight
<img width="800" height="400" alt="image" src="https://github.com/user-attachments/assets/fcd8e66b-47ac-4ac0-8808-9a59a108d71e" />

### TrafficLight(ch03_01)
```
//ch03_01
#define LED_R 12         // 將LED_R 定義為GPIO 12
#define LED_Y 14         // 將LED_Y 定義為GPIO 14
#define LED_G 27         // 將LED_G 定義為GPIO 27

void setup()
{
  // 初始化所有 LED 接腳為輸出模式
  pinMode(LED_R, OUTPUT);
  pinMode(LED_Y, OUTPUT);
  pinMode(LED_G, OUTPUT);
}

void loop()
{
  // 1. 紅燈亮 1 秒
  digitalWrite(LED_R, HIGH);
  digitalWrite(LED_Y, LOW);
  digitalWrite(LED_G, LOW);
  delay(1000);

  // 2. 黃燈亮 1 秒
  digitalWrite(LED_R, LOW);
  digitalWrite(LED_Y, HIGH);
  digitalWrite(LED_G, LOW);
  delay(1000);

  // 3. 綠燈亮 1 秒
  digitalWrite(LED_R, LOW);
  digitalWrite(LED_Y, LOW);
  digitalWrite(LED_G, HIGH);
  delay(1000);
}
```

## 練習1 
4×4薄膜鍵盤_TrafficLight_程式整合：</br>
使用鍵盤1、2、3鍵分別點亮紅、黃、綠燈
```
//ch05_02
//init section




void setup(){
  Serial.begin(115200);




}

void loop(){
//get key value section





    //decide light signal

    if(    ){



    }
    if(    ){



    }
    if(    ){



    }
  } //decide key value section end }
}

```

## 4×4薄膜鍵盤 + LCD1602
<img width="800" height="400" alt="image" src="https://github.com/user-attachments/assets/c85f9dc1-72c3-41c1-87fd-e526a73a78b2" /></br>

> lcd.clear();
>> 清空lcd並設定游標位置至(0,0)第一行第1格開始
>> 可宣告一個變數，用來計算目前輸入到哪個位置，處理換行以及清空

### LCD1602(ch04_01)
```
//ch04_01
#include <LiquidCrystal_I2C.h>      // 引入LiquidCrystal_I2C函式庫，用於控制I2C介面的LCD顯示器
LiquidCrystal_I2C lcd(0x27,16,2);   // 創建LCD物件，設定I2C地址為0x27，並指定 LCD為16列(characters)2行(rows)
int count = 0; 

void setup()
{
  lcd.init();                   // 初始化LCD顯示器
  // 閃爍三次
  for(int i = 0; i < 3; i++) {
    lcd.backlight(); // 開啟背光
    delay(250);
    lcd.noBacklight(); // 關閉背光
    delay(250);
  }
  lcd.backlight(); // 開啟背光
}

void loop()
{
  lcd.setCursor(2,0);           // 設定游標位置至(2,0)第一行第3格開始
  lcd.print("Hello, Wego!");    // 顯示 "Hello, Wego!"
  lcd.setCursor(0,1);           // 設定游標位置至(0,1)第二行第1格開始
  lcd.print("Count:");          // 顯示 "Count:"
  lcd.setCursor(6,1);           // 設定游標位置至(6,1)第二行第7格開始
  lcd.print(count);             // 顯示count值
  delay(1000);
  count++;
}
```

## 練習2
密碼鎖
- 密碼預設為1234，按#字鍵為輸入
- 按*字鍵為清空

'''
#include <Keypad.h>                 // 引入鍵盤函式庫
#include <LiquidCrystal_I2C.h>      // 引入LiquidCrystal_I2C函式庫，用於控制I2C介面的LCD顯示器

LiquidCrystal_I2C lcd(0x27,16,2);   // 創建LCD物件，設定I2C地址為0x27，並指定 LCD為16列(characters)2行(rows)

const byte ROWS = 4;        // 宣告列為4，const為不能被更改，byte數值範圍0~255，占用1 byte記憶體
const byte COLS = 4;        // 宣告行為4，const為不能被更改，byte數值範圍0~255，占用1 byte記憶體
char keys[ROWS][COLS] = {   // 鍵盤對應字元
  {'1','2','3','A'},
  {'4','5','6','B'},
  {'7','8','9','C'},
  {'*','0','#','D'}
};
byte rowPins[ROWS] = {0,4,16,17};   // row接腳
byte colPins[COLS] = {5,18,19,21};  // col接腳
Keypad keynum = Keypad(makeKeymap(keys), rowPins, colPins, ROWS, COLS );  // 創建Keypad物件叫做keynum，並放入上述的資料
int i=0;

String passcode = "1234";   // 預設密碼
String inputCode = "";      // 暫存用戶的按鍵字串
bool acceptKey = true;      // 接受按鍵輸入的變數，預設為「接受」


// 清除單行文字
void clearRow(byte n) {
  byte last = 16 - n;               // 宣告變數last，來判定是否到達最後一格
  lcd.setCursor(n, 1);              // 游標移到(n,1)第2行第n格，也就是"PIN:"之後
  for (byte i = 0; i < last; i++) { // 輸出" "，以達到消除文字的作用
    lcd.print(" ");
  }
  lcd.setCursor(n, 1);              // 游標回到(n,1)第2行第n格，也就是"PIN:"之後
}

// 重設LCD顯示文字和輸入狀態。
void resetLocker() {
  lcd.clear();                     // 清空Lcd
  lcd.print("Please enter PIN");      
  lcd.setCursor(0, 1);             // 切換到第2行  
  lcd.print("PIN:");                 
  lcd.cursor();                    // 顯示游標
  acceptKey = true;                // 接受按鍵輸入
  inputCode = "";                  // 儲存使用者輸入的字串 
}

// 比對輸入的密碼
void checkPinCode() {
  acceptKey = false;               // 不接受按鍵輸入
  clearRow(0);                     // 從第0個字元開始清除LCD畫面
  lcd.noCursor();                  // 隱藏游標
  lcd.setCursor(0, 1);             // 切換到第2行
  
  if (inputCode == passcode) {     // 比對密碼
    lcd.print("Welcome home!");
  } else {
    lcd.print("***WRONG!!***");
  }
  delay(3000);
  resetLocker();                   // 重設LCD顯示文字和輸入狀態
}

void setup() {
// init section






}

void loop() {
  char key = keynum.getKey();
  
  if (acceptKey && key != NO_KEY) {    // 如果目前接受輸入 且 有新的字元輸入
    if (key == '*') {                  // 清除畫面    
      //write some code here
                                // 從第4個字元開始清除
                                // 清空inputCode
    } else if (key == '#') {           // 比對輸入密碼
      //write some code here
      
    } else {
      //write some code here                
                                // 儲存用戶的按鍵字元
                                // 輸入值用*取代
    }
  }
}

'''

<!--
```
//ch05_02sol
#include <Keypad.h>       // 引入鍵盤函式庫      
# define LED_R 12         // 將LED_R 定義為GPIO 12
# define LED_Y 14         // 將LED_Y 定義為GPIO 14
# define LED_G 27         // 將LED_G 定義為GPIO 27


const byte ROWS = 4;      // 宣告列為4，const為不能被更改，byte數值範圍0~255，占用1 byte記憶體
const byte COLS = 4;      // 宣告行為4，const為不能被更改，byte數值範圍0~255，占用1 byte記憶體
char keys[ROWS][COLS] = { // 鍵盤對應字元
  {'1','2','3','A'},
  {'4','5','6','B'},
  {'7','8','9','C'},
  {'*','0','#','D'}
};
byte rowPins[ROWS] = {0,4,16,17};       // row接腳
byte colPins[COLS] = {5,18,19,21};      // col接腳
Keypad keynum = Keypad(makeKeymap(keys), rowPins, colPins, ROWS, COLS );  // 創建Keypad物件叫做keynum，並放入上述的資料

void setup(){
  Serial.begin(115200);
  pinMode(LED_R,OUTPUT);   // LED_R腳位，設定為OUTPUT
  pinMode(LED_Y,OUTPUT);   // LED_Y腳位，設定為OUTPUT
  pinMode(LED_G,OUTPUT);   // LED_G腳位，設定為OUTPUT
}

void loop(){
  char key = keynum.getKey();     // 讀取按鍵值並存入key
  if (key != NO_KEY){             // 判斷是否有按鍵值
    Serial.println(key);          // 在序列埠監控視窗顯示按鍵值
    if(key=='1'){
      digitalWrite(LED_R,HIGH);   // 點亮Red light
      digitalWrite(LED_Y,LOW);    // 關閉Yellow light
      digitalWrite(LED_G,LOW);    // 關閉Green light
    }
    if(key=='2'){
      digitalWrite(LED_R,LOW);    // 關閉Red light
      digitalWrite(LED_Y,HIGH);   // 點亮Yellow light
      digitalWrite(LED_G,LOW);    // 關閉Green light
    }
    if(key=='3'){
      digitalWrite(LED_R,LOW);    // 關閉Red light
      digitalWrite(LED_Y,LOW);    // 關閉Yellow light
      digitalWrite(LED_G,HIGH);   // 點亮Green light
    }
  }
}
```

```
//ch05_03sol
#include <Keypad.h>                 // 引入鍵盤函式庫
#include <LiquidCrystal_I2C.h>      // 引入LiquidCrystal_I2C函式庫，用於控制I2C介面的LCD顯示器

LiquidCrystal_I2C lcd(0x27,16,2);   // 創建LCD物件，設定I2C地址為0x27，並指定 LCD為16列(characters)2行(rows)

const byte ROWS = 4;        // 宣告列為4，const為不能被更改，byte數值範圍0~255，占用1 byte記憶體
const byte COLS = 4;        // 宣告行為4，const為不能被更改，byte數值範圍0~255，占用1 byte記憶體
char keys[ROWS][COLS] = {   // 鍵盤對應字元
  {'1','2','3','A'},
  {'4','5','6','B'},
  {'7','8','9','C'},
  {'*','0','#','D'}
};
byte rowPins[ROWS] = {0,4,16,17};   // row接腳
byte colPins[COLS] = {5,18,19,21};  // col接腳
Keypad keynum = Keypad(makeKeymap(keys), rowPins, colPins, ROWS, COLS );  // 創建Keypad物件叫做keynum，並放入上述的資料
int i=0;

String passcode = "1234";   // 預設密碼
String inputCode = "";      // 暫存使用者輸入的字串
bool acceptKey = true;      // 接受按鍵輸入的變數，預設為「接受」


// 清除單行文字
void clearRow(byte n) {
  byte last = 16 - n;               // 宣告變數last，來判定是否到達最後一格
  lcd.setCursor(n, 1);              // 游標移到(n,1)第2行第n格，也就是"PIN:"之後
  for (byte i = 0; i < last; i++) { // 輸出" "，以達到消除文字的作用
    lcd.print(" ");
  }
  lcd.setCursor(n, 1);              // 游標回到(n,1)第2行第n格，也就是"PIN:"之後
}

// 重設LCD顯示文字和輸入狀態。
void resetLocker() {
  lcd.clear();                     // 清空Lcd
  lcd.print("Please enter PIN");      
  lcd.setCursor(0, 1);             // 切換到第2行  
  lcd.print("PIN:");                 
  lcd.cursor();                    // 顯示游標
  acceptKey = true;                // 接受按鍵輸入
  inputCode = "";                  // 儲存使用者輸入的字串 
}

// 比對輸入的密碼
void checkPinCode() {
  acceptKey = false;               // 不接受按鍵輸入
  clearRow(0);                     // 從第0個字元開始清除LCD畫面
  lcd.noCursor();                  // 隱藏游標
  lcd.setCursor(0, 1);             // 切換到第2行
  
  if (inputCode == passcode) {     // 比對密碼
    lcd.print("Welcome home!");
  } else {
    lcd.print("***WRONG!!***");
  }
  delay(3000);
  resetLocker();                   // 重設LCD顯示文字和輸入狀態
}

void setup() {
  Serial.begin(115200);
  lcd.init();                      // 初始化lcd物件
  lcd.backlight();                 // 開啟背光
  resetLocker();
}

void loop() {
  char key = keynum.getKey();
  
  if (acceptKey && key != NO_KEY) {    // 如果目前接受輸入 且 有新的字元輸入
    if (key == '*') {                  // 清除畫面      
      clearRow(4);                     // 從第4個字元開始清除
      inputCode = "";                  // 清空inputCode
    } else if (key == '#') {           // 比對輸入密碼
      checkPinCode();
    } else {
      inputCode += key;                // 儲存用戶的按鍵字元
      lcd.print('*');
    }
  }
}
```
-->
